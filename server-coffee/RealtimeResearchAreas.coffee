root = exports ? this

class root.RealtimeResearchAreas

	constructor: ->
		@currentAreas = {}
	
	addUserToArea: (area) ->
		console.log("adding user to area")

	removeUserFromArea: (area) ->

	getUserCount: (area) ->
