section#chat
  h2 Chat with other researchers
  
  form.alias-form
    input.alias(type="text", placeholder="enter nickname and hit enter")
    input(type="submit", value="Go")

  ul#messages

  form.chat-form
    input.text-input(type="text", name="", placeholder="chat!")
